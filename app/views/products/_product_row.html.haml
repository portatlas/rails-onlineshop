-# .well
-#   .row
-#     .col-xs-8
-#       %h4
-#     -# = link_to product.name, product_url(product.id)
-#     -# - product.name
-#     .col-xs-4
-#       = form_for :product_order, url: product_orders_path,  remote: true do |f|
-#         %h4.text-right
-#           -# Unit Price:
-#           -# %span{:style => "color: green"}= number_to_currency product.price
-#         .input-group
-#           = f.number_field :number_of_items, value: 1, class: "form-control", min: 1
-#           .input-group-btn
-#             = f.hidden_field :product_id, value: product.id
-#             = f.submit "Add to Cart", class: "btn btn-primary"
