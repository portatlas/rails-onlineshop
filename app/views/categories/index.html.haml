%section.container
  %h1= "Categories"
  - @categories.each do |category|
    %ul.list-group
    %li.list-group-item
      %span.badge
        = category.products.count
      = link_to category.name, category_url(category.id)

